describe('Context', () => {
    describe('creation and basics', () => {
        it('requires a config')

        it('has a name')

        it('has an id based on name and params')
    })

    describe('joining', () => {
        it('multiple sockets can join with correct member administration')

        it('fails on bad auth')

        it('fails if config does not allow user to join')
    })

    describe('requests', () => {
        it('') // HIER VERDER
    })
})